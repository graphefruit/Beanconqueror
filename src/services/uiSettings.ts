/**Core**/
import {Injectable} from '@angular/core';
/**Ionic native**/
import { Storage } from '@ionic/storage';

/**Services**/
import {UIHelper} from '../services/uiHelper';

/**
 * Handles every data which will be stored, beans, extracted coffees
 */
@Injectable()
export class UISettings {


}
